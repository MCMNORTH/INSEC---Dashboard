<header class="bg-[#1E2761] text-white px-6 py-4 flex justify-between items-center"><div><strong class="text-xl">INSEC</strong><span class="text-blue-200 ml-3">{{ $espace }}</span></div><div class="flex items-center gap-3"><a href="{{ route('alertes.index') }}" class="text-sm bg-white/10 px-3 py-2 rounded"><i class="fa-solid fa-bell mr-1"></i> Alertes @php($n = auth()->user()->alertes()->where('active', true)->whereNull('lue_at')->whereNull('archivee_at')->count()) @if($n)<span class="ml-1 bg-red-500 rounded-full px-2 py-0.5">{{ $n }}</span>@endif</a><form method="POST" action="{{ route('logout') }}">@csrf<button class="text-sm bg-white/10 px-3 py-2 rounded">Déconnexion</button></form></div></header>
