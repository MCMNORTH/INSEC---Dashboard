<x-app-layout><div class="flex min-h-screen bg-gray-100">@include('partials.sidebar')<main class="flex-1 p-6"><div class="flex flex-wrap justify-between gap-3 mb-5"><div><h1 class="text-xl font-bold text-[#1E2761]">Examens & résultats</h1><p class="text-sm text-gray-500">Planification, convocations et validation des UE</p></div><a href="{{ route('examens.create') }}" class="bg-amber-500 text-white px-4 py-2 rounded-lg">+ Planifier un examen</a></div>
@if(session('status'))<div class="bg-green-50 text-green-700 p-3 rounded mb-4">{{ session('status') }}</div>@endif
<form method="GET" class="bg-white p-3 rounded-xl shadow mb-4"><select name="annee_id" onchange="this.form.submit()" class="border-gray-300 rounded-lg"><option value="">Toutes les années</option>@foreach($annees as $annee)<option value="{{ $annee->id }}" @selected(request('annee_id')==$annee->id)>{{ $annee->libelle }}</option>@endforeach</select></form>
<div class="bg-white rounded-xl shadow overflow-hidden"><table class="w-full text-sm"><thead class="bg-[#1E2761] text-white"><tr><th class="p-3 text-left">Date</th><th class="p-3 text-left">Diplôme / UE</th><th class="p-3 text-left">Session</th><th class="p-3 text-left">Salle</th><th class="p-3 text-left">Convoqués</th><th class="p-3 text-left">Statut</th></tr></thead><tbody>@forelse($examens as $examen)<tr class="border-b hover:bg-gray-50 cursor-pointer" onclick="location='{{ route('examens.show',$examen) }}'"><td class="p-3">{{ $examen->date_examen->format('d/m/Y H:i') }}</td><td class="p-3"><strong>{{ $examen->ue?->formation?->code }} · {{ $examen->ue?->code }}</strong><br><span class="text-gray-500">{{ $examen->ue?->libelle }}</span></td><td class="p-3">{{ $examen->session }}<br><span class="text-xs text-gray-500">{{ $examen->anneeAcademique?->libelle }}</span></td><td class="p-3">{{ $examen->salle ?? '—' }}</td><td class="p-3">{{ $examen->resultats_count }}</td><td class="p-3">{{ $examen->statut }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-gray-400">Aucun examen planifié.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $examens->links() }}</div></main></div></x-app-layout>
