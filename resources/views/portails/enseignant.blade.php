<x-app-layout><div class="min-h-screen bg-gray-100">@include('portails._header',['espace'=>'Espace enseignant'])<main class="max-w-6xl mx-auto p-6"><h1 class="text-2xl font-bold text-[#1E2761]">Bonjour {{ $enseignant->prenom }} {{ $enseignant->nom }}</h1><p class="text-gray-500 mb-6">{{ $enseignant->specialite }}</p><h2 class="font-bold text-[#1E2761] mb-3">Mes UE</h2><div class="grid md:grid-cols-3 gap-4 mb-7">@forelse($enseignant->affectations as $affectation)<div class="bg-white rounded-xl shadow p-4"><strong>{{ $affectation->ue?->code }}</strong><p>{{ $affectation->ue?->libelle }}</p><p class="text-xs text-gray-500">{{ $affectation->ue?->formation?->code }} · {{ $affectation->nombre_etudiants }} étudiants</p></div>@empty<p class="text-gray-500">Aucune UE affectée.</p>@endforelse</div><h2 class="font-bold text-[#1E2761] mb-3">Examens de mes UE</h2><div class="bg-white rounded-xl shadow overflow-hidden"><table class="w-full text-sm"><thead class="bg-[#1E2761] text-white"><tr><th class="p-3 text-left">UE</th><th class="p-3 text-left">Date</th><th class="p-3 text-left">Session</th><th class="p-3 text-left">Étudiants</th></tr></thead><tbody>@forelse($examens as $examen)<tr class="border-b"><td class="p-3">{{ $examen->ue?->code }} · {{ $examen->ue?->libelle }}</td><td class="p-3">{{ $examen->date_examen->format('d/m/Y H:i') }}</td><td class="p-3">{{ $examen->session }}</td><td class="p-3">{{ $examen->resultats_count }}</td></tr>@empty<tr><td colspan="4" class="p-6 text-center text-gray-500">Aucun examen.</td></tr>@endforelse</tbody></table></div></main></div></x-app-layout>
