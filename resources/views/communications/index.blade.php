<x-app-layout><div class="flex min-h-screen bg-gray-100">@include('partials.sidebar')<main class="flex-1 p-6 lg:p-8"><div class="mb-6"><p class="text-sm text-gray-500">Traçabilité</p><h1 class="text-2xl font-bold text-[#1E2761]">Communications par e-mail</h1></div>
<section class="grid grid-cols-3 gap-4 mb-6">@foreach([['Envoyé','text-green-700','bg-green-50'],['Échec','text-red-700','bg-red-50'],['En attente','text-amber-700','bg-amber-50']] as $s)<div class="{{ $s[2] }} border border-white rounded-xl p-4"><p class="text-2xl font-bold {{ $s[1] }}">{{ $stats[$s[0]]??0 }}</p><p class="text-sm text-gray-500">{{ $s[0] }}</p></div>@endforeach</section>
<form method="GET" class="mb-4"><select name="statut" onchange="this.form.submit()" class="rounded-lg border-gray-300 text-sm"><option value="">Tous les statuts</option>@foreach(['Envoyé','Échec','En attente'] as $s)<option @selected(request('statut')===$s)>{{ $s }}</option>@endforeach</select></form>
<div class="bg-white border rounded-xl overflow-hidden"><table class="w-full text-sm"><thead class="bg-gray-50 text-gray-500"><tr><th class="text-left p-4">Date</th><th class="text-left p-4">Destinataire</th><th class="text-left p-4">Type / sujet</th><th class="text-left p-4">Statut</th></tr></thead><tbody>@forelse($journaux as $j)<tr class="border-t"><td class="p-4 whitespace-nowrap">{{ $j->created_at->format('d/m/Y H:i') }}</td><td class="p-4"><strong>{{ $j->nom_destinataire?:'—' }}</strong><span class="block text-xs text-gray-400">{{ $j->destinataire }}</span></td><td class="p-4"><span class="text-xs text-gray-400">{{ $j->type }}</span><span class="block">{{ $j->sujet }}</span></td><td class="p-4"><span class="rounded-full px-3 py-1 text-xs {{ $j->statut==='Envoyé'?'bg-green-100 text-green-700':($j->statut==='Échec'?'bg-red-100 text-red-700':'bg-amber-100 text-amber-700') }}">{{ $j->statut }}</span>@if($j->erreur)<span title="{{ $j->erreur }}" class="ml-2 text-red-500">ⓘ</span>@endif</td></tr>@empty<tr><td colspan="4" class="p-10 text-center text-gray-400">Aucun e-mail enregistré.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $journaux->links() }}</div></main></div></x-app-layout>
