<x-app-layout><div class="flex min-h-screen bg-gray-100">@include('partials.sidebar')<main class="flex-1 p-6 max-w-4xl"><a href="{{ route('etudiants.show',$inscription->etudiant) }}" class="text-sm text-gray-500">&larr; Retour à la fiche</a><h1 class="text-xl font-bold text-[#1E2761] mt-2 mb-6">Modifier l’inscription</h1>@if($errors->any())<div class="bg-red-50 text-red-700 p-3 rounded mb-4">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('inscriptions.update',$inscription) }}" class="bg-white rounded-xl shadow p-6 space-y-5">@csrf @method('PUT') @include('inscriptions._fields',['showStatus'=>true])<button class="bg-amber-500 text-white px-4 py-2 rounded-lg">Enregistrer</button></form></main></div></x-app-layout>
