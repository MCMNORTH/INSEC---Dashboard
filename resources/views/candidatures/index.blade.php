<x-app-layout><div class="flex min-h-screen bg-gray-100">@include('partials.sidebar')<main class="flex-1 p-6 lg:p-8">@include('partials.student-section-nav')<div class="flex justify-between items-end mb-6"><div><p class="text-sm text-gray-500">Admissions</p><h1 class="text-2xl font-bold text-[#1E2761]">Candidatures</h1></div><a href="{{ route('candidatures.create') }}" target="_blank" class="bg-[#1E2761] text-white px-4 py-2 rounded-lg text-sm">Ouvrir le formulaire public</a></div>
<form method="GET" class="mb-4"><select name="statut" onchange="this.form.submit()" class="rounded-lg border-gray-300 text-sm"><option value="">Tous les statuts</option>@foreach(['Nouvelle','En étude','Admissible','Rejetée','Inscrite'] as $s)<option @selected(request('statut')===$s)>{{ $s }}</option>@endforeach</select></form>
<div class="bg-white border rounded-xl overflow-hidden"><table class="w-full text-sm"><thead class="bg-gray-50 text-gray-500"><tr><th class="text-left p-4">Référence / candidat</th><th class="text-left p-4">Diplôme</th><th class="text-left p-4">Année</th><th class="text-left p-4">Statut</th><th></th></tr></thead><tbody>@forelse($candidatures as $c)<tr class="border-t"><td class="p-4"><strong>{{ $c->prenom }} {{ $c->nom }}</strong><span class="block text-xs text-gray-400">{{ $c->reference }} · {{ $c->email }}</span></td><td class="p-4 font-semibold">{{ $c->formation->code }}</td><td class="p-4">{{ $c->anneeAcademique->libelle }}</td><td class="p-4"><span class="rounded-full bg-gray-100 px-3 py-1 text-xs">{{ $c->statut }}</span></td><td class="p-4 text-right"><a href="{{ route('candidatures.show',$c) }}" class="font-semibold text-[#1E2761]">Examiner →</a></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-gray-400">Aucune candidature.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $candidatures->links() }}</div></main></div></x-app-layout>
